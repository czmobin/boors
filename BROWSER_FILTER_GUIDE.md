# 🌐 راهنمای نصب و استفاده از فیلتر بورس روی وبسایت

این راهنما نحوه استفاده از فیلتر حرفه‌ای بورس **مستقیماً روی سایت TSETMC** را توضیح می‌دهد.

---

## 🎯 دو روش استفاده

### روش 1️⃣: **Console (ساده‌تر)** ⭐ پیشنهادی
کپی-پیست مستقیم در Console مرورگر - نیازی به نصب ندارد

### روش 2️⃣: **Userscript (دائمی)**
نصب به عنوان افزونه - یکبار نصب، همیشه کار می‌کند

---

## 🚀 روش 1: استفاده با Console (توصیه می‌شود)

### مرحله 1: باز کردن سایت
1. به سایت بورس بروید: **http://www.tsetmc.com/**
2. یا **http://old.tsetmc.com/**

### مرحله 2: باز کردن Console
یکی از این روش‌ها را انتخاب کنید:

**روش الف - کلید میانبر:**
- ویندوز/لینوکس: `F12` یا `Ctrl + Shift + J`
- مک: `Cmd + Option + J`

**روش ب - منو:**
1. کلیک راست روی صفحه
2. انتخاب "Inspect" یا "بازرسی"
3. رفتن به تب "Console"

### مرحله 3: کپی کردن کد
1. فایل `tsetmc_filter_console.js` را باز کنید
2. **همه کد را** کپی کنید (Ctrl+A → Ctrl+C)

### مرحله 4: اجرای کد
1. در Console، کد را **Paste** کنید (Ctrl+V)
2. `Enter` بزنید
3. باید این پیام‌ها را ببینید:
   ```
   ✅ فیلتر حرفه‌ای بورس آماده است!
   🔍 دکمه فیلتر در گوشه پایین-راست صفحه ظاهر شده است
   ```

### مرحله 5: استفاده
1. دکمه 🔍 در **گوشه پایین-راست** ظاهر می‌شود
2. روی آن کلیک کنید
3. از فیلترها استفاده کنید!

---

## ⚙️ روش 2: نصب دائمی با Tampermonkey

### مرحله 1: نصب Tampermonkey

**کروم/اج:**
https://chrome.google.com/webstore/detail/tampermonkey/dhdgffkkebhmkfjojejmpbldmpobfkfo

**فایرفاکس:**
https://addons.mozilla.org/en-US/firefox/addon/tampermonkey/

**اوپرا:**
https://addons.opera.com/en/extensions/details/tampermonkey-beta/

**سافاری:**
https://www.tampermonkey.net/

### مرحله 2: ایجاد اسکریپت جدید

1. روی آیکون Tampermonkey کلیک کنید
2. "Dashboard" را انتخاب کنید
3. تب "Utilities" → "Import from URL" یا
4. دکمه "➕" (New script) را بزنید

### مرحله 3: کپی کردن کد

1. فایل `tsetmc_filter_userscript.js` را باز کنید
2. **همه کد را** کپی کنید
3. در ویرایشگر Tampermonkey **Paste** کنید
4. `Ctrl+S` یا File → Save بزنید

### مرحله 4: فعال‌سازی

1. مطمئن شوید اسکریپت **Enabled** است (سبز)
2. به سایت TSETMC بروید
3. صفحه را Refresh کنید (F5)
4. دکمه 🔍 باید ظاهر شود

---

## 📋 فیلترهای موجود

### 1️⃣ فیلترهای آماده (10 عدد)

| فیلتر | توضیح |
|-------|-------|
| 💰 **ورود پول حقیقی** | نمادهای با حجم بالا و رشد قیمت (تقریبی) |
| 📈 **قدرت خرید** | نمادهای با حجم و رشد قیمت مثبت |
| 📊 **حجم بالا** | حجم بیشتر از 2 برابر میانگین بازار |
| 🚀 **رشد قیمت قوی** | رشد بیش از 2% |
| 📉 **افت قیمت** | افت بیش از 2% |
| 🧠 **پول هوشمند** | حجم بالا + رشد قوی |
| ✅ **همه مثبت** | قیمت، حجم، ارزش همگی مثبت |
| 🔥 **صف خرید** | نمادهای در حداکثر قیمت |
| 💎 **ارزش بالا** | بیش از 10 میلیارد تومان |
| ⚡ **معاملات فعال** | بیش از 500 معامله |

### 2️⃣ فیلتر سفارشی

می‌توانید فیلتر سفارشی با این پارامترها بسازید:

- **تغییر قیمت** (درصد): مثلاً 3 → نمادهای با رشد بیش از 3%
- **حجم** (حداقل): مثلاً 1000000 → حجم بیش از 1 میلیون
- **ارزش** (میلیارد تومان): مثلاً 5 → ارزش بیش از 5 میلیارد
- **تعداد معاملات**: مثلاً 300 → بیش از 300 معامله

---

## 🎨 نحوه استفاده

### استفاده از فیلتر آماده

1. دکمه 🔍 را بزنید
2. روی یکی از فیلترها کلیک کنید
3. منتظر بمانید تا داده‌ها بارگذاری شود (معمولاً 2-5 ثانیه)
4. نتایج نمایش داده می‌شود

### استفاده از فیلتر سفارشی

1. پنل فیلتر را باز کنید
2. به پایین بروید به بخش "⚙️ فیلتر سفارشی"
3. نوع فیلتر را انتخاب کنید
4. آستانه را وارد کنید
5. "اعمال سفارشی" را بزنید

### مثال فیلتر سفارشی

**هدف**: نمادهای با رشد قیمت بیش از 5%

1. نوع: **تغییر قیمت (درصد)**
2. آستانه: **5**
3. کلیک روی "اعمال سفارشی"

**نتیجه**: لیست نمادهایی که بیش از 5% رشد کرده‌اند

---

## 📊 اطلاعات نمایش داده شده

برای هر نماد این اطلاعات نمایش داده می‌شود:

```
1. وبملت 📈
   💵 قیمت: 15,420 ریال
   📈 تغییر: +3.25%
   📦 حجم: 1,250,000
   💰 ارزش: 12.50 میلیارد
   🔢 تعداد: 850 معامله
```

### توضیح آیکون‌ها:

- 💵 **قیمت**: آخرین قیمت معامله شده
- 📈/📉 **تغییر**: درصد تغییر نسبت به دیروز (سبز=مثبت، قرمز=منفی)
- 📦 **حجم**: تعداد سهام معامله شده
- 💰 **ارزش**: ارزش کل معاملات (میلیارد تومان)
- 🔢 **تعداد**: تعداد دفعات معامله

---

## 🔧 تنظیمات و سفارشی‌سازی

### تغییر تعداد نتایج

در کد، این خط را پیدا کنید:
```javascript
displayResults(filtered.slice(0, 25), ...
```

عدد `25` را به تعداد دلخواه تغییر دهید (مثلاً 50):
```javascript
displayResults(filtered.slice(0, 50), ...
```

### تغییر موقعیت دکمه

در بخش CSS، این قسمت را پیدا کنید:
```css
#bfilter-toggle-btn {
    bottom: 20px;
    right: 20px;
    ...
}
```

برای انتقال به گوشه چپ:
```css
#bfilter-toggle-btn {
    bottom: 20px;
    left: 20px;    /* تغییر از right به left */
    ...
}
```

### تغییر رنگ‌ها

رنگ اصلی (بنفش):
```css
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
```

می‌توانید `#667eea` و `#764ba2` را به رنگ‌های دلخواه تغییر دهید.

---

## ⚠️ نکات مهم

### 1️⃣ محدودیت‌های فیلتر

این اسکریپت از **API عمومی TSETMC** استفاده می‌کند که:

- ✅ داده‌های قیمت، حجم، ارزش را دارد
- ❌ داده‌های حقیقی/حقوقی مستقیم ندارد (تقریبی محاسبه می‌شود)
- ❌ اندیکاتورهای پیشرفته (RSI, MACD) ندارد

برای فیلترهای پیشرفته‌تر، از نسخه Python استفاده کنید.

### 2️⃣ سرعت

- داده‌ها مستقیماً از TSETMC دریافت می‌شود
- معمولاً 2-5 ثانیه طول می‌کشد
- در ساعات شلوغی ممکن است کندتر باشد

### 3️⃣ رفرش دستی

داده‌ها **خودکار بروز نمی‌شوند**. برای داده‌های جدید:

- دوباره روی فیلتر کلیک کنید
- یا صفحه را رفرش کنید (F5)

### 4️⃣ مرورگرها

✅ **تست شده:**
- گوگل کروم
- مایکروسافت اج
- فایرفاکس
- اوپرا

⚠️ **ممکن است کار نکند:**
- اینترنت اکسپلورر (قدیمی)

---

## 🐛 عیب‌یابی

### مشکل 1: دکمه 🔍 ظاهر نمی‌شود

**راه‌حل:**
1. صفحه را رفرش کنید (F5)
2. Console را بررسی کنید (F12) - آیا خطایی وجود دارد؟
3. مطمئن شوید کد **کامل** کپی شده
4. از حالت **Incognito/Private** امتحان کنید

### مشکل 2: خطای "CORS" یا "Mixed Content"

**دلیل:** TSETMC از HTTP استفاده می‌کند، نه HTTPS

**راه‌حل:**
- به `http://www.tsetmc.com` بروید (نه https://)
- یا از `http://old.tsetmc.com` استفاده کنید

### مشکل 3: نتایج خالی است

**دلایل احتمالی:**
- بازار بسته است
- اتصال اینترنت ضعیف است
- API سایت در دسترس نیست

**راه‌حل:**
- در ساعات بازار امتحان کنید (9-12:30 روزهای کاری)
- اینترنت را بررسی کنید
- چند دقیقه بعد دوباره تلاش کنید

### مشکل 4: اسکریپت کار نمی‌کند (Tampermonkey)

**راه‌حل:**
1. Dashboard → Scripts → مطمئن شوید Enabled است
2. بررسی کنید `@match` شامل URL فعلی است
3. اسکریپت را پاک و دوباره اضافه کنید

---

## 💡 نکات و ترفندها

### ترفند 1: ترکیب فیلترها

1. ابتدا فیلتر "حجم بالا" را اجرا کنید
2. از نتایج، نمادهایی که رشد قیمت دارند را انتخاب کنید
3. روی هر نماد کلیک کنید → صفحه جزئیات باز می‌شود

### ترفند 2: ذخیره نتایج

نتایج را می‌توانید:
1. اسکرین‌شات بگیرید
2. کپی-پیست کنید در Notepad
3. عکس بگیرید با موبایل

### ترفند 3: استفاده همزمان چند فیلتر

می‌توانید در چند تب مختلف، چند فیلتر مختلف اجرا کنید:

- تب 1: فیلتر "رشد قیمت"
- تب 2: فیلتر "حجم بالا"
- تب 3: فیلتر "ارزش بالا"

### ترفند 4: بوکمارک کردن Console Script

می‌توانید کد را در یک فایل HTML ذخیره کنید:

```html
<!DOCTYPE html>
<html>
<head>
    <title>فیلتر بورس</title>
</head>
<body>
    <h1>فیلتر بورس</h1>
    <p>کد زیر را کپی کنید:</p>
    <textarea rows="10" cols="80" id="code">
        [کد JavaScript شما]
    </textarea>
    <button onclick="document.getElementById('code').select(); document.execCommand('copy'); alert('کپی شد!');">کپی</button>
</body>
</html>
```

---

## 🔄 بروزرسانی

### بروزرسانی Console Script

هر بار که سایت را باز می‌کنید، باید دوباره کد را Paste کنید.

**روش سریع:**
1. کد را در یک فایل `.txt` ذخیره کنید
2. هر بار از فایل کپی کنید

### بروزرسانی Userscript

1. Dashboard Tampermonkey
2. پیدا کردن اسکریپت
3. کلیک روی "Edit"
4. کد جدید را Paste کنید
5. Save

---

## 📈 استراتژی‌های پیشنهادی

### استراتژی 1: صبح بازار (9:00-9:30)

```
1. فیلتر "همه مثبت"
2. فیلتر "رشد قیمت قوی"
3. بررسی نمادهای مشترک
```

### استراتژی 2: میانه روز (10:30-11:30)

```
1. فیلتر "حجم بالا"
2. فیلتر "ارزش بالا"
3. بررسی نمادهای با فعالیت بالا
```

### استراتژی 3: انتهای بازار (12:00-12:30)

```
1. فیلتر "صف خرید"
2. فیلتر "پول هوشمند"
3. آماده‌سازی برای فردا
```

---

## 🆚 مقایسه دو روش

| ویژگی | Console | Tampermonkey |
|-------|---------|--------------|
| **آسانی نصب** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| **دائمی بودن** | ❌ هر بار باید اجرا شود | ✅ یکبار نصب |
| **سرعت شروع** | ⭐⭐⭐⭐⭐ سریع | ⭐⭐⭐⭐ |
| **بروزرسانی** | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **پشتیبانی** | همه مرورگرها | نیاز به افزونه |

**توصیه:**
- برای **آزمایش**: Console
- برای **استفاده روزانه**: Tampermonkey

---

## 📞 پشتیبانی

### سوالات متداول

**س: آیا رایگان است؟**
ج: بله، کاملاً رایگان و متن‌باز

**س: آیا نیاز به ثبت‌نام دارد؟**
ج: خیر، هیچ ثبت‌نامی لازم نیست

**س: آیا داده‌های من ذخیره می‌شود؟**
ج: خیر، همه چیز در مرورگر شما اجرا می‌شود

**س: آیا می‌توانم کد را تغییر دهم؟**
ج: بله، کاملاً آزاد و قابل تغییر است

---

## 🎯 نتیجه

این فیلتر یک ابزار **رایگان** و **ساده** برای فیلتر کردن نمادهای بورس روی خود سایت TSETMC است.

**مزایا:**
- ✅ بدون نصب برنامه
- ✅ بدون سرور
- ✅ بدون هزینه
- ✅ سریع و آسان
- ✅ قابل سفارشی‌سازی

**محدودیت‌ها:**
- ⚠️ داده‌های حقیقی/حقوقی تقریبی است
- ⚠️ اندیکاتورهای پیشرفته ندارد
- ⚠️ نیاز به اجرای دستی (روش Console)

**برای فیلترهای پیشرفته‌تر**، از نسخه Python (`professional_filter.py`) استفاده کنید که:
- RSI, MFI دارد
- داده‌های دقیق حقیقی/حقوقی
- ذخیره در Excel
- فیلترهای بیشتر

---

**موفق باشید! 🚀📈💰**
