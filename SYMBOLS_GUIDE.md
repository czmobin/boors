# راهنمای تنظیم نمادها

## روش‌های مختلف انتخاب نمادها

### 1️⃣ پردازش همه نمادها (1369 نماد)

برای پردازش **تمام نمادهای بورس** از این فایل استفاده کنید:

```bash
cp symbols_all.json symbols.json
```

محتوای `symbols_all.json`:
```json
{
  "process_all": true,
  "symbols": []
}
```

**مزایا:**
- تمام نمادهای بورس پردازش می‌شوند
- فرصت‌های بیشتر کشف می‌شود
- نیازی به انتخاب دستی ندارید

**معایب:**
- زمان پردازش بیشتر (حدود 1-2 دقیقه)
- فایل Excel بزرگ‌تر
- حجم داده زیاد

---

### 2️⃣ انتخاب نمادهای خاص

فایل `symbols.json` را با نمادهای دلخواه پر کنید:

```json
{
  "process_all": false,
  "symbols": [
    {
      "name": "گروه مالي نماد غدير",
      "ticker": "نماد",
      "id": null
    },
    {
      "name": "صندوق طلای عیار مفید",
      "ticker": "عیار",
      "id": null
    }
  ]
}
```

**مزایا:**
- سریع‌تر (فقط نمادهای مورد نظر)
- فایل Excel کوچک‌تر
- تمرکز روی نمادهای خاص

**معایب:**
- باید نمادها را دستی اضافه کنید
- ممکن است فرصت‌هایی را از دست بدهید

---

### 3️⃣ استفاده از فایل‌های از پیش ساخته شده

بعد از اجرای `python generate_symbols.py` (نیاز به API key):

```bash
# 100 نماد پرمعامله
cp symbols_top100.json symbols.json

# همه نمادها
cp symbols_all.json symbols.json

# نمادهای یک دسته خاص
cp symbols_سهام.json symbols.json
```

---

## نحوه پیدا کردن ticker نمادها

### روش 1: از طریق ربات تلگرام

```
/search <نام نماد>

مثال:
/search غدیر
/search خودرو
/search پارسان
```

خروجی:
```
🔎 نتایج جستجو برای 'غدیر':

1. نماد
   📌 نام: گروه مالي نماد غدير
   🔢 کد: 34144...
```

**ticker = نماد** (همون ردیف اول)

---

### روش 2: از طریق سایت TSETMC

1. برو به https://tsetmc.com
2. جستجو کن نماد رو
3. `ticker` همون نام کوتاه نماده (مثلا "نماد", "عیار", "خودرو")

---

### روش 3: از طریق اسکریپت Python

```python
from brsapi_client import BrsApiClient

client = BrsApiClient()
all_data = client.get_all_symbols()

# جستجو در نام
search_term = "غدیر"
for symbol in all_data:
    if search_term in symbol.get('l30', ''):
        print(f"نام: {symbol['l30']}")
        print(f"ticker: {symbol['l18']}")
        print()
```

---

## فرمت فیلدها در symbols.json

```json
{
  "name": "نام کامل شرکت",
  "ticker": "نماد کوتاه (اجباری)",
  "id": null or شماره (اختیاری)
}
```

**فیلدهای اجباری:**
- `ticker`: نماد کوتاه (مثل "نماد", "عیار") - **حتما باید درست باشد**

**فیلدهای اختیاری:**
- `name`: نام کامل - فقط برای نمایش
- `id`: شماره ID - اگر نمی‌دونید null بذارید

---

## مثال‌های کاربردی

### مثال 1: فقط صندوق‌های طلا

```json
{
  "symbols": [
    {"ticker": "عیار", "name": "صندوق طلای عیار", "id": null},
    {"ticker": "زر", "name": "صندوق طلای زر", "id": null},
    {"ticker": "پاسار", "name": "صندوق طلای پاسارگاد", "id": null}
  ]
}
```

### مثال 2: نمادهای پتروشیمی

```json
{
  "symbols": [
    {"ticker": "شپنا", "name": "پالایش نفت اصفهان", "id": null},
    {"ticker": "شبندر", "name": "پتروشیمی بندر امام", "id": null},
    {"ticker": "فارس", "name": "پتروشیمی فارس", "id": null}
  ]
}
```

### مثال 3: نمادهای خودرویی

```json
{
  "symbols": [
    {"ticker": "خودرو", "name": "ایران خودرو", "id": null},
    {"ticker": "خساپا", "name": "سایپا", "id": null},
    {"ticker": "خپارس", "name": "پارس خودرو", "id": null}
  ]
}
```

---

## نکات مهم

### ✅ درست

```json
{"ticker": "نماد", "name": "گروه مالي نماد غدير", "id": null}
```

### ❌ غلط

```json
// ticker اشتباه
{"ticker": "غدیر", "name": "...", "id": null}

// فیلد ticker نداره
{"name": "گروه مالي نماد غدير", "id": null}

// ticker با فاصله
{"ticker": "نماد غدیر", "name": "...", "id": null}
```

---

## توصیه‌ها

### برای شروع (تازه‌واردها):
```bash
cp symbols_all.json symbols.json
```
همه نمادها رو ببین و بعد تصمیم بگیر.

### برای کاربران حرفه‌ای:
لیست نمادهای خودت رو بساز با ticker های دقیق.

### برای سرعت بالا:
فقط 10-20 نماد انتخاب کن که واقعاً مهم هستند.

---

## رفع مشکل

### خطا: نماد یافت نشد

**علت:** ticker اشتباه است

**راه‌حل:**
1. از ربات تلگرام جستجو کن: `/search نام_نماد`
2. ticker دقیق رو کپی کن
3. در symbols.json استفاده کن

### خطا: زمان پردازش زیاد

**علت:** تعداد نمادها زیاد است

**راه‌حل:**
- از `process_all: false` استفاده کن
- فقط نمادهای مهم رو انتخاب کن
- یا صبور باش، اولین بار کنده ولی بعداً cache میشه

---

## فایل‌های موجود

```
symbols.json           - فایل اصلی که استفاده می‌شود
symbols_all.json       - پردازش همه نمادها
symbols_example.json   - نمونه برای شروع
symbols_top100.json    - 100 نماد برتر (بعد از generate)
```

---

## سوالات متداول

**س: چند نماد می‌تونم انتخاب کنم؟**
ج: هر تعداد! از 1 تا 1369

**س: ticker رو از کجا پیدا کنم؟**
ج: `/search` در ربات تلگرام یا https://tsetmc.com

**س: id چیه؟**
ج: شماره یکتای نماد، اختیاری هست، null بذار

**س: آیا می‌تونم فقط دسته خاصی رو انتخاب کنم؟**
ج: بله، با `python generate_symbols.py --category`

**س: چطور بفهمم ticker درسته؟**
ج: اجرا کن، اگر "نماد یافت نشد" گفت، اشتباهه

---

برای اطلاعات بیشتر:
- [README.md](README.md)
- [TELEGRAM_BOT_GUIDE.md](TELEGRAM_BOT_GUIDE.md)
