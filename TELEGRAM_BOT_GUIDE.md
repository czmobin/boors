# راهنمای ربات تلگرام فیلتر نمادهای بورس

## مراحل راه‌اندازی

### 1. ساخت ربات در تلگرام

1. به ربات [@BotFather](https://t.me/BotFather) در تلگرام پیام دهید
2. دستور `/newbot` را بزنید
3. یک نام برای ربات انتخاب کنید (مثال: `Bourse Filter Bot`)
4. یک username انتخاب کنید که باید به `bot` ختم شود (مثال: `my_bourse_filter_bot`)
5. BotFather یک **توکن** به شما می‌دهد. این توکن را کپی کنید

**مثال توکن:**
```
1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

### 2. پیدا کردن Chat ID خود

1. به ربات [@userinfobot](https://t.me/userinfobot) پیام دهید
2. ربات Chat ID شما را نمایش می‌دهد (مثال: `123456789`)
3. این عدد را یادداشت کنید

### 3. تنظیم فایل محیطی (.env)

فایل `.env` در پوشه پروژه بسازید:

```bash
# کپی کردن فایل نمونه
cp .env.example .env
```

محتوای فایل `.env`:

```env
# توکن ربات از BotFather
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# Chat ID های مجاز (با کاما جدا شوند)
AUTHORIZED_USERS=123456789,987654321
```

**نکته مهم:** فایل `.env` را به git اضافه نکنید (در `.gitignore` هست)

### 4. نصب کتابخانه‌ها

```bash
pip install -r requirements.txt
```

### 5. راه‌اندازی ربات

```bash
python telegram_bot.py
```

خروجی:
```
🤖 ربات شروع به کار کرد...
📊 تعداد نمادهای تحت پوشش: 1
🔐 کاربران مجاز: [123456789]
```

## دستورات ربات

### دستورات اصلی

#### `/start`
شروع کار با ربات و نمایش پیام خوش‌آمدگویی

**مثال:**
```
/start
```

**خروجی:**
```
🤖 سلام! من ربات فیلتر نمادهای بورس هستم.

📋 دستورات موجود:
/scan - اسکن همه نمادها
/filter - نمایش نمادهای فیلتر شده
...
```

---

#### `/scan`
اسکن کامل همه نمادها و محاسبه قدرت خریدار

**ویژگی‌ها:**
- اولین اسکن روز به عنوان مبنا ذخیره می‌شود
- نمایش 10 نماد برتر بر اساس قدرت خریدار
- ذخیره خودکار در Excel

**مثال:**
```
/scan
```

**خروجی:**
```
✅ اسکن اول روز انجام شد و به عنوان مبنا ذخیره شد

🕐 زمان: 09:15:30
📊 تعداد نمادهای اسکن شده: 100

🔝 10 نماد برتر بر اساس قدرت خریدار:

1. نماد غدیر
   💪 قدرت خریدار: 2.5
   💰 ورود پول: 1,234,567 م

2. ...
```

---

#### `/filter`
نمایش نمادهایی که قدرت خریدارشان نسبت به ابتدای روز رشد کرده (بیش از 10%)

**پیش‌نیاز:** حتما یک بار `/scan` زده باشید

**مثال:**
```
/filter
```

**خروجی:**
```
🎯 نمادهای فیلتر شده (5 نماد)
🕐 زمان: 11:30:00

1. نماد غدیر
   📈 رشد: 25.5%
   💪 قدرت اولیه: 2.00
   💪 قدرت فعلی: 2.51
   💰 ورود پول: 1,500,000 م

2. ...
```

**اگر نمادی پیدا نشد:**
```
📭 هیچ نمادی با رشد بیش از 10% پیدا نشد.

💡 این می‌تونه به این دلایل باشه:
- هنوز تغییر قابل توجهی نداشتیم
- بازار نزولی هست
- باید بیشتر صبر کنیم
```

---

#### `/excel`
دریافت فایل Excel امروز با تمام داده‌ها

**ویژگی‌های فایل:**
- شیت‌های زمان‌بندی شده (هر اسکن یک شیت)
- شیت ویژه "فیلتر_شده" برای نمادهای با رشد

**مثال:**
```
/excel
```

**خروجی:**
ربات فایل Excel را ارسال می‌کند

---

#### `/reset`
پاک کردن داده‌های پایه روز و شروع دوباره

**کاربرد:**
- وقتی می‌خواهید از زمان جدیدی شروع کنید
- برای رفع مشکلات داده‌های اشتباه

**مثال:**
```
/reset
```

**خروجی:**
```
🔄 داده‌های پایه روز پاک شد.

اسکن بعدی به عنوان مبنای جدید ذخیره خواهد شد.
برای شروع: /scan
```

---

#### `/symbols`
نمایش لیست نمادهای تحت پوشش

**مثال:**
```
/symbols
```

**خروجی:**
```
📋 نمادهای تحت پوشش (100 نماد):

1. گروه مالي نماد غدير (نماد)
2. پالایش نفت اصفهان (شپنا)
3. ...
```

---

#### `/search <نام>`
جستجوی نماد و پیدا کردن insCode

**کاربرد:**
- پیدا کردن کد نماد برای افزودن به `symbols.json`

**مثال:**
```
/search غدیر
```

**خروجی:**
```
🔎 نتایج جستجو برای 'غدیر':

1. نماد
   📌 نام: گروه مالي نماد غدير
   🔢 کد: 65197850030400728

2. ...
```

---

#### `/stats`
نمایش آمار کلی سیستم

**مثال:**
```
/stats
```

**خروجی:**
```
📊 آمار سیستم

📋 تعداد نمادها: 100
📅 تاریخ: 2025-12-31
🕐 ساعت: 10:30:15
✅ داده پایه: موجود (100 نماد)
📁 فایل Excel: 45.2 KB
```

---

#### `/help`
نمایش راهنمای کامل دستورات

---

## سناریوهای استفاده

### سناریو 1: استفاده روزانه عادی

**صبح (9:00 - شروع بازار):**
```
/scan
```
→ داده‌های اولیه ذخیره می‌شود

**ظهر (10:30):**
```
/scan
/filter
```
→ ببینید کدام نمادها رشد کرده‌اند

**نزدیک پایان بازار (12:00):**
```
/scan
/filter
/excel
```
→ فایل Excel را دریافت کنید

---

### سناریو 2: پایش لحظه‌ای

هر 15-30 دقیقه:
```
/scan
/filter
```

---

### سناریو 3: جستجو و افزودن نماد جدید

```
/search خودرو
```

کد نماد را کپی کنید و به `symbols.json` اضافه کنید:
```json
{
  "name": "ایران خودرو",
  "insCode": "کد_از_جستجو",
  "ticker": "خودرو"
}
```

ربات را restart کنید:
```bash
# Ctrl+C در ترمینال
python telegram_bot.py
```

---

## اجرای ربات به صورت دائمی

### روش 1: استفاده از screen (ساده)

```bash
# ایجاد session
screen -S bourse_bot

# اجرای ربات
python telegram_bot.py

# خروج بدون بستن (Ctrl+A سپس D)
```

برای برگشت:
```bash
screen -r bourse_bot
```

---

### روش 2: استفاده از systemd (پیشرفته - لینوکس)

فایل `/etc/systemd/system/bourse-bot.service`:

```ini
[Unit]
Description=Bourse Telegram Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/boors
Environment="PATH=/usr/bin:/usr/local/bin"
ExecStart=/usr/bin/python3 /path/to/boors/telegram_bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

فعال‌سازی:
```bash
sudo systemctl daemon-reload
sudo systemctl enable bourse-bot
sudo systemctl start bourse-bot
```

بررسی وضعیت:
```bash
sudo systemctl status bourse-bot
```

مشاهده لاگ‌ها:
```bash
sudo journalctl -u bourse-bot -f
```

---

### روش 3: استفاده از Docker

فایل `Dockerfile`:

```dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .

CMD ["python", "telegram_bot.py"]
```

فایل `docker-compose.yml`:

```yaml
version: '3.8'

services:
  bourse-bot:
    build: .
    container_name: bourse_bot
    restart: always
    env_file:
      - .env
    volumes:
      - ./output:/app/output
      - ./symbols.json:/app/symbols.json
```

اجرا:
```bash
docker-compose up -d
```

---

## امنیت

### محدود کردن دسترسی

فقط کاربران مشخص شده در `AUTHORIZED_USERS` می‌توانند از ربات استفاده کنند.

**پیدا کردن Chat ID:**
```
/start
```

اگر مجاز نباشید:
```
❌ شما مجاز به استفاده از این ربات نیستید.
User ID شما: 123456789
```

این ID را به `.env` اضافه کنید.

---

### محافظت از توکن

- **هرگز** توکن ربات را در GitHub یا جای عمومی قرار ندهید
- فایل `.env` را به `.gitignore` اضافه کنید (از قبل اضافه شده)
- در صورت لو رفتن توکن، آن را در BotFather revoke کنید

---

## رفع مشکلات رایج

### ربات پاسخ نمی‌دهد

**بررسی‌ها:**
1. آیا ربات در حال اجرا است؟
```bash
ps aux | grep telegram_bot
```

2. آیا توکن درست است؟
```bash
cat .env | grep TOKEN
```

3. آیا Chat ID شما در لیست مجازها هست؟

---

### خطای "Not Found" یا "Unauthorized"

- توکن اشتباه است
- ربات در BotFather disable شده
- توکن منقضی شده

**راه‌حل:** توکن جدید از BotFather دریافت کنید

---

### ربات خیلی کند است

- تعداد نمادها زیاد است (100+)
- API های TSETMC کند هستند
- اتصال اینترنت ضعیف است

**راه‌حل:** صبر کنید، سیستم retry خودکار دارد

---

### فایل Excel ارسال نمی‌شود

**بررسی:**
```bash
ls -lh output/
```

اگر فایل وجود ندارد، ابتدا `/scan` بزنید

---

## نکات مهم

1. **اسکن اول روز:** همیشه صبح اول بازار یک `/scan` بزنید
2. **محدودیت API:** بین درخواست‌ها 0.5 ثانیه تاخیر هست
3. **حافظه:** فایل‌های Excel روزانه حذف نمی‌شوند، دستی پاک کنید
4. **ریست روزانه:** هر روز صبح `/reset` بزنید برای شروع تازه

---

## مثال‌های عملی

### صبح (9:00)
```
شما: /reset
ربات: 🔄 داده‌های پایه روز پاک شد.

شما: /scan
ربات: ✅ اسکن اول روز انجام شد...
```

### 30 دقیقه بعد (9:30)
```
شما: /scan
ربات: ✅ اسکن انجام شد...

شما: /filter
ربات: 🎯 نمادهای فیلتر شده (3 نماد)...
```

### پایان روز (12:30)
```
شما: /excel
ربات: [ارسال فایل Excel]
```

---

## پشتیبانی

برای مشکلات و سوالات:
- بررسی لاگ‌های ترمینال
- بررسی فایل `.env`
- مطمئن شوید `symbols.json` معتبر است

---

## ویژگی‌های آینده (پیشنهادی)

- [ ] اضافه/حذف نماد از طریق ربات
- [ ] تنظیم درصد فیلتر سفارشی
- [ ] هشدار خودکار برای نمادهای خاص
- [ ] نمودار قدرت خریدار
- [ ] اسکن خودکار دوره‌ای
- [ ] پشتیبانی از چند کاربر با دیتابیس جداگانه
