# اسکنر خودکار بورس (بدون تلگرام)

دو اسکریپت ساده برای اسکن خودکار و ذخیره در Excel

## 📁 فایل‌ها

### 1️⃣ `scan_once.py` - اسکن یکبار (برای تست)

```bash
python3 scan_once.py
```

**کاربرد:**
- یک بار اسکن می‌کنه
- نتیجه رو در Excel ذخیره می‌کنه
- برای تست و اجرای دستی

---

### 2️⃣ `auto_scanner.py` - اسکنر خودکار (هر 1 دقیقه)

```bash
python3 auto_scanner.py
```

**کاربرد:**
- هر 1 دقیقه اسکن می‌کنه
- فقط در ساعات بورس کار می‌کنه (شنبه-چهارشنبه 9:00-12:30)
- داده‌ها رو به صورت time-series در Excel ذخیره می‌کنه
- برای اجرای مداوم روی سرور

**توقف:**
```
Ctrl+C
```

---

## ⚙️ تنظیمات

نمادهای مورد نظر رو در `symbols.json` تنظیم کن:

```json
{
  "process_all": true,
  "symbols": [
    {"ticker": "نماد"},
    {"ticker": "عیار"},
    {"ticker": "کچاد"}
  ]
}
```

- `process_all: true` = همه 1369 نماد اسکن می‌شن
- فقط نمادهای لیست بالا در Excel ذخیره می‌شن

---

## 📊 خروجی Excel

**ساختار:**
```
📁 output/bourse_data_2026-01-04.xlsx
  📄 Sheet: نماد
     زمان      | قدرت | ورود_پول | قیمت | ...
     09:05:00  | 2.45 | 125.50   | 1500 | ...
     09:10:00  | 2.67 | 180.75   | 1505 | ...
     09:15:00  | 3.12 | 250.30   | 1510 | ...

  📄 Sheet: عیار
     زمان      | قدرت | ورود_پول | قیمت | ...
     09:05:00  | 1.89 | -50.25   | 2300 | ...
     09:10:00  | 1.95 | 30.50    | 2305 | ...
```

**ویژگی‌ها:**
- هر نماد = یک شیت
- هر اسکن = یک ردیف جدید
- رکوردها append می‌شن (پاک نمی‌شن)
- history کامل روز

---

## 🚀 اجرای در Background (Linux)

### با nohup:
```bash
nohup python3 auto_scanner.py > scanner.log 2>&1 &
```

### با screen:
```bash
screen -S scanner
python3 auto_scanner.py
# Ctrl+A,D برای detach
```

### با systemd (service):

فایل `/etc/systemd/system/bourse-scanner.service`:

```ini
[Unit]
Description=Bourse Auto Scanner
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/boors
ExecStart=/usr/bin/python3 /root/boors/auto_scanner.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

فعال‌سازی:
```bash
sudo systemctl daemon-reload
sudo systemctl enable bourse-scanner
sudo systemctl start bourse-scanner
sudo systemctl status bourse-scanner
```

لاگ‌ها:
```bash
sudo journalctl -u bourse-scanner -f
```

---

## 📋 Requirements

```bash
pip install -r requirements.txt
```

کتابخانه‌های مورد نیاز:
- schedule
- pandas
- openpyxl
- pytz
- requests

---

## 🔍 مثال خروجی

```
============================================================
🤖 اسکنر خودکار بورس راه‌اندازی شد
============================================================
📊 تعداد نمادها برای اسکن: همه (1369)
📋 نمادها برای Excel: {'نماد', 'عیار', 'کچاد'}
📁 مسیر فایل Excel: output/bourse_data_2026-01-04.xlsx
⏰ برنامه: هر 1 دقیقه در ساعات بورس
🕐 ساعات کاری: 9:00 - 12:30 (شنبه تا چهارشنبه)
============================================================

🚀 اجرای اسکن اولیه...

============================================================
🔄 شروع اسکن - 2026-01-04 09:05:15
============================================================
📡 در حال دریافت داده‌های تمام نمادها از BrsApi...
✅ 1369 نماد دریافت شد
✅ پردازش کامل شد: 1369 نماد
✅ داده‌های baseline ذخیره شد (زمان: 09:05)
✅ 3 نماد در Excel ذخیره شد - زمان: 09:05:15

📊 خلاصه اسکن:
  • کل نمادهای پردازش شده: 1369
  • نمادهای ذخیره شده در Excel: نماد, عیار, کچاد
  • زمان baseline: 09:05

✅ اسکن کامل شد - 09:05:15
============================================================

✅ اسکنر خودکار فعال شد
⏰ اسکن بعدی: 1 دقیقه دیگر

💡 برای توقف: Ctrl+C
============================================================
```

---

## 💡 نکات

1. **اولین اسکن روز** به عنوان baseline ذخیره می‌شه (ساعت 9:05)
2. **همه اسکن‌ها** با این baseline مقایسه می‌شن
3. **فایل Excel** هر روز جدید ساخته می‌شه
4. **داده‌ها** append می‌شن، پاک نمی‌شن
5. **خارج از ساعات بورس** اسکن نمی‌شه

---

## ❓ سوالات متداول

**Q: چرا فقط چند تا نماد در Excel ذخیره می‌شن؟**
A: فقط نمادهایی که در `symbols.json` تعریف کردی ذخیره می‌شن.

**Q: چطوری نماد اضافه کنم؟**
A: در `symbols.json` اضافه کن:
```json
{"ticker": "نماد_جدید"}
```

**Q: چرا مقادیر تغییر نمی‌کنن؟**
A: مطمئن شو که `force_refresh=True` هست (الان هست).

**Q: چطوری scheduler رو ببینم؟**
A: خروجی console رو ببین - هر اسکن چاپ می‌شه.

**Q: می‌تونم زمان اسکن رو عوض کنم؟**
A: در `auto_scanner.py` خط زیر رو تغییر بده:
```python
schedule.every(5).minutes.do(...)  # 5 رو عوض کن
```

---

## 🎯 مقایسه با تلگرام بات

| ویژگی | تلگرام بات | auto_scanner.py |
|-------|------------|-----------------|
| اجرا | نیاز به bot token | مستقل |
| کنترل | از طریق تلگرام | Console |
| فیلتر | دارد | ندارد (فقط ذخیره) |
| نمودار | دارد | ندارد |
| اسکن خودکار | ✅ | ✅ |
| ذخیره Excel | ✅ | ✅ |
| سادگی | متوسط | ساده |
| مناسب برای | کاربر عادی | سرور/اتوماسیون |
