# راهنمای استفاده کامل

## مراحل راه‌اندازی

### 1. نصب پیش‌نیازها

```bash
pip install -r requirements.txt
```

### 2. افزودن نمادها

دو روش برای افزودن نمادها وجود دارد:

#### روش الف: استفاده از ابزار جستجو

```bash
python find_symbol.py غدیر
```

خروجی به شکل زیر خواهد بود:

```
1. نماد
   نام کامل: گروه مالي نماد غدير
   کد نماد: 65197850030400728
   URL: https://tsetmc.com/instInfo/65197850030400728

فرمت JSON برای افزودن به symbols.json:
{
  "name": "گروه مالي نماد غدير",
  "insCode": "65197850030400728",
  "ticker": "نماد"
}
```

#### روش ب: دستی از سایت TSETMC

1. به [TSETMC](https://tsetmc.com) بروید
2. نماد مورد نظر را جستجو کنید
3. از URL صفحه نماد، `insCode` را کپی کنید
   - مثال URL: `https://tsetmc.com/instInfo/65197850030400728`
   - insCode: `65197850030400728`

### 3. ویرایش فایل symbols.json

```json
{
  "symbols": [
    {
      "name": "گروه مالي نماد غدير",
      "insCode": "65197850030400728",
      "ticker": "نماد"
    },
    {
      "name": "پالایش نفت اصفهان",
      "insCode": "کد_نماد_را_اینجا_بگذارید",
      "ticker": "شپنا"
    }
  ]
}
```

### 4. اجرا

#### اجرای یک بار (تست)

```bash
python main.py --once
```

#### اجرای دوره‌ای (هر 5 دقیقه)

```bash
python main.py
```

#### اجرای با فاصله زمانی دلخواه (مثلا 10 دقیقه)

```bash
python main.py --interval 10
```

## توضیحات شاخص‌های خروجی

### 1. قدرت خریدار (buyer_power_ratio)

- **فرمول**: `حجم خرید حقوقی / حجم فروش حقوقی`
- **تفسیر**:
  - بیشتر از 1: حقوقی‌ها خریدار هستند
  - کمتر از 1: حقوقی‌ها فروشنده هستند
  - برابر 1: تعادل

**مثال**:
- قدرت خریدار = 2.5 → حقوقی‌ها 2.5 برابر بیشتر خریده‌اند

### 2. ورود پول حقوقی (legal_money_flow)

- **فرمول**: `(حجم خرید - حجم فروش) / 1,000,000`
- **واحد**: میلیون تومان
- **تفسیر**:
  - مثبت: ورود پول حقوقی
  - منفی: خروج پول حقوقی

### 3. ورود پول حقیقی (real_money_flow)

- **فرمول**: `(حجم فروش - حجم خرید) / 1,000,000` (معکوس)
- **واحد**: میلیون تومان
- **تفسیر**:
  - مثبت: ورود پول حقیقی (معمولا همراه با خروج حقوقی)
  - منفی: خروج پول حقیقی

### 4. سرانه خرید/فروش

- **فرمول**: `حجم کل / تعداد معاملات`
- **کاربرد**: نشان‌دهنده اندازه متوسط معاملات

**مثال**:
- سرانه خرید = 50 میلیون → هر معامله خرید به طور متوسط 50 میلیون تومان بوده

### 5. رشد قدرت خریدار (فقط در شیت فیلتر شده)

- **فرمول**: `((قدرت فعلی - قدرت اولیه) / قدرت اولیه) × 100`
- **واحد**: درصد
- **کاربرد**: نشان‌دهنده تغییر روند در طول روز

## فیلترینگ نمادها

برنامه به طور خودکار نمادهایی را که قدرت خریدارشان نسبت به ابتدای روز بیش از 10% رشد کرده، فیلتر می‌کند.

برای تغییر این مقدار، فایل `config.py` را ویرایش کنید:

```python
MIN_BUYER_POWER_GROWTH = 0.1  # 10%
```

## ساعات اجرا

توصیه می‌شود برنامه را در ساعات زیر اجرا کنید:

- **شروع**: 9:00 صبح (ابتدای معاملات)
- **پایان**: 12:30 ظهر (پایان معاملات)

**نکته**: اولین اجرای روز به عنوان مبنای مقایسه ذخیره می‌شود.

## مثال سناریو استفاده

### سناریوی 1: پایش 50 نماد برتر

1. 50 نماد برتر بورس را به `symbols.json` اضافه کنید
2. برنامه را ساعت 9:00 اجرا کنید
3. هر 5 دقیقه داده‌ها بروزرسانی می‌شود
4. در پایان روز، فایل Excel حاوی:
   - حدود 42 شیت (هر 5 دقیقه یک شیت در 3.5 ساعت)
   - شیت فیلتر شده با نمادهایی که رشد داشته‌اند

### سناریوی 2: رصد نمادهای خاص

1. فقط نمادهای موردنظر خود را اضافه کنید (مثلا 5-10 نماد)
2. برنامه را با فاصله کوتاه‌تر اجرا کنید:
   ```bash
   python main.py --interval 1
   ```
3. رصد دقیق‌تر تغییرات دقیقه‌ای

## خطاهای رایج و راه‌حل

### 1. خطای Timeout

```
HTTPSConnectionPool: Read timed out
```

**راه‌حل**:
- اتصال اینترنت خود را بررسی کنید
- برنامه خودکار retry دارد
- در صورت ادامه مشکل، سایت TSETMC ممکن است down باشد

### 2. هیچ داده‌ای دریافت نشد

**راه‌حل**:
- مطمئن شوید در ساعات معاملات هستید
- `insCode` نمادها را بررسی کنید

### 3. فایل Excel ایجاد نمی‌شود

**راه‌حل**:
- مطمئن شوید پوشه `output` دسترسی نوشتن دارد
- کتابخانه `openpyxl` را بررسی کنید:
  ```bash
  pip install --upgrade openpyxl
  ```

## نکات پیشرفته

### اجرا به صورت Background

```bash
nohup python main.py > bourse.log 2>&1 &
```

### اجرا با systemd (لینوکس)

فایل `/etc/systemd/system/bourse-filter.service`:

```ini
[Unit]
Description=Bourse Stock Filter
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/boors
ExecStart=/usr/bin/python3 /path/to/boors/main.py
Restart=always

[Install]
WantedBy=multi-user.target
```

سپس:

```bash
sudo systemctl enable bourse-filter
sudo systemctl start bourse-filter
```

### اجرا با cron

```bash
# اجرا هر روز ساعت 9 صبح
0 9 * * 1-5 cd /path/to/boors && python3 main.py --interval 5
```

## تحلیل داده‌ها در Excel

### ایجاد نمودار خطی قدرت خریدار

1. شیت‌های مختلف را انتخاب کنید
2. ستون `قدرت_خریدار` را برای یک نماد انتخاب کنید
3. Insert → Chart → Line Chart
4. روند تغییرات قدرت خریدار در طول روز نمایش داده می‌شود

### فیلتر پیشرفته

از قابلیت Filter در Excel برای:
- نمایش فقط نمادهایی با ورود پول مثبت
- مرتب‌سازی بر اساس قدرت خریدار
- فیلتر بر اساس حجم معاملات

## پشتیبانی

برای گزارش مشکل یا پیشنهاد:
- Issue در GitHub
- یا بررسی کد و رفع مشکل به صورت محلی
